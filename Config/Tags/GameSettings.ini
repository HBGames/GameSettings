; Copyright Hitbox Games, LLC. All Rights Reserved.
;
; LEGACY. Setting identity is now an FPrimaryAssetId taken from the
; contribution DataAsset (see Docs/IDENTITY_AND_ASSETS.md); tabs, sections,
; and rows are no longer keyed by gameplay tag. The two root tags below are
; left in place for back-compat but nothing in the plugin uses them. They
; can be deleted once no project content references Settings.Tab.* tags.
;
; Tags that DO still matter live elsewhere: a Game Setting Action's
; NamedAction tag (defined by the contributor) and the Platform.Trait.* tags
; an edit condition gates on (defined by CommonUI).

[/Script/GameplayTags.GameplayTagsList]
+GameplayTagList=(Tag="Settings",DevComment="LEGACY, unused. Identity moved to FPrimaryAssetId. See Docs/IDENTITY_AND_ASSETS.md.")
+GameplayTagList=(Tag="Settings.Tab",DevComment="LEGACY, unused. Tabs are GameSettingsTab DataAssets keyed by FPrimaryAssetId now. See Docs/IDENTITY_AND_ASSETS.md.")
